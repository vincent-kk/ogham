/**
 * Build the SessionStart banner payload.
 *
 * Skeleton stub: returns a fixed banner. Replace with real context assembly
 * once the plugin's session-start policy is defined (mirror the cogair hook).
 */
export function buildBanner(): string {
  return '[prawf] plugin skeleton active.';
}
