## Purpose

레이어 필터링 모듈. 볼트 설정 상한과 호출별 필터의 교집합을 계산하고 결과를 필터링한다.

## Structure

- `layer-guard/` — 2단계 레이어 필터링 로직

## Boundaries

### Always do

- 모든 툴 호출에 레이어 가드를 적용한다
- 교집합이 빈 경우 볼트 레이어로 폴백한다

### Ask first

- 필터링 로직 변경
- 새로운 필터 유형 추가

### Never do

- 순환 의존성 도입
- 레이어 필터링 우회
