#!/usr/bin/env node
import{existsSync as o}from"node:fs";import{join as n}from"node:path";function r(t){return o(n(t,".filid"))||o(n(t,"CLAUDE.md"))}var u=["[FCA-AI Plan Compliance Checklist]","Before implementation, ensure the plan includes:","1. CLAUDE.md/SPEC.md updates for ALL affected fractal modules","2. SPEC.md updates BEFORE code changes (spec-driven)","3. CLAUDE.md boundary updates if module responsibilities change","4. New fractal modules: CLAUDE.md (max 100 lines, 3-tier) + SPEC.md","If missing, revise the plan to include document update steps."].join(`
`);function i(t){return r(t.cwd)?{continue:!0,hookSpecificOutput:{additionalContext:u}}:{continue:!0}}var s=[];for await(let t of process.stdin)s.push(t);var c=Buffer.concat(s).toString("utf-8"),e;try{let t=JSON.parse(c);e=i(t)}catch{e={continue:!0}}process.stdout.write(JSON.stringify(e));
