/**
 * Cross-layer handoff token types — re-exported from constants/handoff-tokens.ts.
 * See `skills/filid-pipeline/stages.md` for the token policy.
 */
export type { FixRequestType, DebtAction, PipelineStage } from '../constants/handoff-tokens.js';
