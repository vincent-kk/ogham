/**
 * Cross-layer handoff token types — re-exported from constants/handoffTokens.ts.
 * See `skills/pipeline/stages.md` for the token policy.
 */
export type {
  FixRequestType,
  DebtAction,
  PipelineStage,
} from '../constants/handoffTokens.js';
