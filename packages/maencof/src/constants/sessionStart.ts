/** Hard upper bound (Unicode code points) for meta-skill body before silent skip. */
export const META_SKILL_MAX_CHARS = 2500;

/** XML tag wrapping meta-skill body in SessionStart `additionalContext`. */
export const META_SKILL_TAG = 'maencof-meta-skill';
