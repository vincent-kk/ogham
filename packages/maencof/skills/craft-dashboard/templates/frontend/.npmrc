# Supply-chain guard: install only package versions published more than N days ago.
# Helps reduce exposure to freshly published malicious packages.
# Available in npm 11.6+ (older npm versions silently ignore this).
# Unit: days.
min-release-age=7
