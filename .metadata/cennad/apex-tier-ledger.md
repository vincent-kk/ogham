# apex tier + liveness timeout — progress ledger

계획: [apex-tier-plan.md](./apex-tier-plan.md). 한 줄 = 완료된 태스크(무엇이·어디에·어떻게 검증).

| Task                                                 | 상태 | 기록                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — `spawnCli` idle timeout                          | 완료 | `shared/cross-platform/src/spawn/{types,spawnCli}.ts` 에 `idleTimeoutMs`(청크마다 리셋) + `SpawnResult.timeoutKind` 추가, fixture `__tests__/fixtures/heartbeat.mjs` 신규. 선행 red 2건(idle 미동작·timeoutKind undefined) 관찰 후 `yarn workspace @ogham/cross-platform test:run` → 284/284 green. `src/spawn/INTENT.md` Conventions 갱신                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2 — apex 티어 (타입·기본값·병합)                     | 완료 | `types/{conversation,dispatch,config}.ts`(Tier.Apex·세 ModelMap·`TimeoutsConfigSchema`, `spawn_timeout_ms` 제거), `constants/defaults.ts`(apex 기본값 + `timeouts`), `configManager/utils/{mergeModelMap,mergeTimeouts,mergeWithDefaults}.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3 — MCP 도구 timeout 배선                            | 완료 | `mcp/tools/{startConversation,continueConversation}` 이 해석된 tier 로 `hard_cap_ms[tier]` 를 골라 `idleTimeoutMs`/`hardCapMs` 로 전달                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 4 — dispatcher 3종                                   | 완료 | 공통 `dispatcher/utils/timeoutError.ts`(idle/ceiling 메시지 분기); codex 배선만, claude 는 `--output-format stream-json --verbose` + `findResultEvent`/`parseResult` JSONL 재작성, agy 는 stream-json + `--print-timeout`(`printTimeout.ts`) + `findAgyResult` 폴백 유지. 테스트 픽스처 9종 갱신(fake agy 에 stream-json 모드·legacy-text 폴백 케이스 추가). `yarn cennad typecheck` 통과, `yarn cennad test:run` → 682/682 green                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 5 — 설정 UI (apex·timeout·비율바)                    | 완료 | `index.html` 3 provider × apex 행 + default-tier 라디오, "CLI liveness limits" 섹션(분 단위 idle 1 + ceiling 4); `app.js` 는 `tierSelects(prefix)` 로 per-tier 컨트롤을 TIERS 에서 파생(3티어 하드코딩 제거) + `minutesFromMs`/`msFromMinutes` 단일 변환쌍; `styles.css` 비율바는 border-radius shorthand → 논리 속성(단일 세그먼트가 first+last 라 뒤 규칙이 앞 규칙을 통째로 덮던 결함). 실측: 로컬 설정 서버 + playwright 로 단일 세그먼트 computed `border-radius: 999px` 확인, apex 행·idle 10분·caps 360/120/60/30분 렌더 확인                                                                                                                                                                                                                                                                                                                                                                                                           |
| 6 — skills·courier·문서                              | 완료 | SKILL.md 4종 Tier 섹션을 복잡도 루브릭으로 교체(+`--tier apex\|high\|mid\|low`), crosscheck 는 비용 곱셈 경고, courier 는 apex 의미·장시간 실행 수용, `createServer.ts` 도구 describe 2곳. 문서: `CLAUDE.md`(Tier·liveness·레거시 키), dispatcher/settings `INTENT.md`·`DETAIL.md`, `.metadata/cennad/{storage,mcp-tools,provider-dispatch,spec,architecture,skills,web-ui}.md`. 옛 티어 정책 문구를 고정하던 `skill-contract.acceptance.test.ts` 단언을 새 계약으로 갱신                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 8 — codex 실패 사유 배선 (추가)                      | 완료 | 사용자 요청으로 codex 를 실호출하다 발견: usage-limit 실패는 exit 1 + stderr 에 stdin 안내뿐이고 진짜 사유는 stdout JSONL 에만 있어 `unknown` + 무의미한 메시지로 나갔다. `jsonlParser` 가 `errorMessage`(`error`/`turn.failed`)를 추출하고, `mapError` 에 `cliMessage` 를 추가해 분류(합본 패턴 + `usage limit`)와 메시지 양쪽에서 stderr 보다 우선하도록. 선행 red 2건 관찰 후 green; spec 은 15케이스 캡 때문에 `errorMapCliMessage.test.ts` 로 분리                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 9 — agy conversation id (추가)                       | 완료 | 실측으로 `--conversation <id>` 가 **다른 cwd 에서도** 문맥을 유지함을 확인(8371 회수, num_turns 2). `findAgyConversationId` 신규 → `callAgy` 가 반환 → start 가 `externalSessionRef` 로 기록 → `buildResumeArgs` 가 UUID 면 `--conversation`, 레거시 cwd ref 면 `--continue`. cwd 격리는 id 미확보 경로(구버전 agy·transcript 복구) 때문에 유지                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 10 — 설정 UI 2차 (추가)                              | 완료 | "CLI liveness limits" 를 `<details>` 로 접고 summary 우측에 유효값 요약(`renderTimeoutSummary`, 사람이 읽는 단위) — 기본값 사용 유도. 숫자 input 전역 spinner 제거. 실측: 빌드 산출물을 루프백으로 서빙해 `open:false` · summary `idle 10 min · ceilings 6 h / 2 h / 1 h / 30 min` · `appearance:textfield` · apex 행 바인딩(`opus[1m] / max`, `gpt-5.6-sol / ultra`) 확인                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 11 — 비율바 구조 수정 (추가)                         | 완료 | 근본 원인: segment 모서리를 DOM 위치(`:first/:last-child`)로 정하는데 handle 이 같은 부모의 마지막 자식이 되어 끝 segment 가 각졌다(실측: `claude=0px`). 2 레이어로 분리 — segment 는 `#ratio-bar-track`(pill + `overflow:hidden`) 안, handle 은 그 위 — 하고 segment 위치 규칙을 **삭제**. handle 은 트랙보다 크게(12×36) 해 레버로 읽히게. `PROVIDERS` 를 카드 순서(claude·codex·antigravity)로 맞추고 spec 2건으로 고정(순서 일치·위치 규칙 부재). 실측: 1·2·3 provider 및 handle 을 1% 끝까지 드래그한 상태 모두 pill 유지·잘림 없음                                                                                                                                                                                                                                                                                                                                                                                                       |
| 12 — 실 CLI 검증에서 발견한 agy 결함 2건 (추가)      | 완료 | 재시작 후 세 provider 실호출 중 antigravity 만 `unknown` 실패. 원인 1: `agy models` 가 **kebab slug(effort 내장)** 를 주는데 `modelAlias` 가 `(effort)` 를 덧붙여 `gemini-3.6-flash-medium (High)` → agy `invalid model selection`. 실측 6조합으로 계약 확정 후 `isCompleteName` 도입(괄호 포함 또는 공백 없음 → 그대로), 설정 UI 도 `isCompleteAgyName` 으로 완전한 이름 옆 effort 저장 금지(`buildTierConfig` fallback 좁힘). 원인 2: agy 도 codex 처럼 실패 사유를 **stdout 에만** 싣고 stderr 는 빈다 → `findAgyError` + `cliMessage` 배선, classify 에 `invalid model selection` → `cli_error`. 검증: 새 번들을 stdio 로 띄워 **사용자 실제 config** 로 start→resume 성공(`stored`→`8362`, turn 2), 세션 기록의 `external_session_ref` = conversation id·`model` = slug 단독                                                                                                                                                              |
| 13 — agy 모델 축 분리 (추가)                         | 완료 | 재시작 후 세 provider 재확인(agy `stored`→`7745` turn 2 · codex `codex-ok` · claude `claude-ok`). 설정 UI 결함: `agy models` 가 slug 를 주는데 `parseAgyModel` 은 괄호만 분해해 slug 가 통째로 model 축에 들어가고 effort 축이 전부 `(no effort)` 로 죽었다. variant 어휘를 `constants/agyModels.ts` 로 세우고 `parseAgyModel` 이 slug 접미사도 분해 → `agyModelBases`/`agyEffortSet` 은 그대로 따라옴; 디스크 값도 로드 시 정규화(완전한 이름이 목록 밖으로 튀지 않게), `keptAgyEffort` 로 "카탈로그 미독해(보존)" vs "variant 없는 모델(제거)" 구분. dispatch 는 base 표기에 맞춰 재조합(`base (Variant)` / `base-variant`). settingsPage spec 이 16케이스로 캡 초과라 비율바 4건을 `settingsRatioBar.test.ts` 로 분리. 실측: 4 tier 모두 model=base·effort=[high,medium,low] 활성, `claude-sonnet-4-6` 은 effort 비활성; apex 왕복 성공 후 세션 기록 `model: gemini-3.6-flash-high`                                                         |
| 14 — 리뷰 findings 처리 (codex+antigravity 독립 2건) | 완료 | **수정 6건**: (H) 표시명 config 저장 시 agy effort 소실·apex/high 붕괴 → `keptAgyEffort` 가 "카탈로그에 없는 base"는 저장값 보존(재현→수정→재현 확인); joinAgyName 후 effort 잔존이 옛 dispatcher 에서 `<name> (medium)` 이중결합 → `delete effort`(옛 resolver 코드로 검증); timeout 사유가 `spawnError` 에만 있어 유실 → `mapError` 폴백 추가(선행 red); win32 `osTimeout` ×3 이 설정값을 부풀리고 agy `--print-timeout` 과 어긋남 → 60s 이상 미스케일; 응답 없는 stream 이 raw JSONL 로 반환 → `isAgyStream` 판별(선행 red); SKILL 템플릿이 `--continue` 에도 tier 주입 → 주석 3종 수정. **부분 수용 1건**: 레거시 `spawn_timeout_ms` 값 승계는 거부(wall-clock↔liveness 의미 상이) 대신 prune 시 1회 경고. **반박 1건**: spawnCli spec 은 13→15 로 캡 이내(리뷰는 16 주장). **범위 밖 1건**: `mergeModelMap.test.ts` 18케이스는 변경 전에도 18(사전 존재). 검증: repo typecheck clean · repo 5029 passed · cennad 703 · real-CLI E2E 65/65 |
| 7 — 통합 검증                                        | 완료 | cross-platform 284/284 · cennad 692/692 · E2E 62 passed(3 skipped) · real-CLI claude 경로 성공(codex/antigravity 는 사용자 config 에서 disabled 라 미실행) · `yarn lint` 신규 경고 0 · `build:plugin` 훅 캡 가드 통과 · `claude --model "opus[1m]"` 수용 확인                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
