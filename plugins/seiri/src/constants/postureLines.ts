/** Optional assistance posture returned by explicit settings queries. */
export const WORKFLOW_CHAIN_LINE =
  'Workflow assistance is available only after explicit task participation. Skill selection and scope remain with the host, user, and agent.';

/** Strict preserves the same participation boundary; it never widens skill selection. */
export const STRICT_POSTURE_LINE =
  'Use verification evidence appropriate to the claim; reuse it while scope and artifact remain unchanged.';
