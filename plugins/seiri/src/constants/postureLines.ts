/** Strict preserves the same participation boundary; it never widens skill selection. */
export const STRICT_POSTURE_LINE =
  'Posture (strict): borderline moments inside an active task go to their owner skill; completion claims name the verification run that backs them.';
