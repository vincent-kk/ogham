{
  "name": "@ogham/maencof",
  "version": "0.10.3",
  "description": "Personal knowledge space manager with markdown-based knowledge graph, spreading activation search, and memory lifecycle management",
  "keywords": [
    "claude-code",
    "plugin",
    "knowledge-graph",
    "memory",
    "spreading-activation",
    "markdown"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/vincent-kk/ogham.git",
    "directory": "plugins/maencof"
  },
  "license": "MIT",
  "author": {
    "name": "Vincent K. Kelvin",
    "email": "lunox273@gmail.com"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "agents",
    "bridge",
    "libs",
    "hooks",
    "skills",
    "templates",
    ".claude-plugin",
    "plugin.json",
    ".codex-plugin",
    ".mcp.json",
    "mcp_config.json",
    "hooks.json",
    "README.md"
  ],
  "scripts": {
    "bench": "vitest bench",
    "bench:run": "vitest bench --run",
    "build": "yarn clean && yarn version:sync && yarn build:compile && yarn build:mcp && yarn build:hooks && yarn build:compile-plugin",
    "build:compile": "tsc -p tsconfig.build.json",
    "build:mcp": "node scripts/buildMcpServer.mjs",
    "build:hooks": "node scripts/buildHooks.mjs",
    "build:compile-plugin": "node --import tsx ../../tools/plugin-compiler/src/main.ts sync .",
    "build:plugin": "yarn build:mcp && yarn build:hooks",
    "clean": "rm -rf bridge",
    "dev": "tsc --watch",
    "eval": "vitest run src/__tests__/eval",
    "format": "prettier --write \"src/**/*.ts\" \"*.{json,mjs,md}\"",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "version:major": "yarn version major",
    "version:minor": "yarn version minor",
    "version:patch": "yarn version patch",
    "version:sync": "node ../../scripts/inject-version.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "~1.22.0",
    "fast-glob": "^3.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@ogham/cross-platform": "workspace:^",
    "@ogham/session-finalizer": "workspace:^",
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^4.1.10",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
