/** 일일 롤업 `filePaths` 합집합 상한 (노이즈·크기 제어). */
export const MAX_ROLLUP_PATHS = 50;

/** work-index 하위 daily rollup 디렉터리 이름. */
export const DAILY_ROLLUP_SUBDIR = 'daily';

/** 토픽 역색인 파일 이름. */
export const TOPIC_INDEX_FILE = 'topic-index.json';

/** 레이어 역색인 파일 이름. */
export const LAYER_INDEX_FILE = 'layer-index.json';
