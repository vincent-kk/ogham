export const MAENCOF_DIR = '.maencof';
export const MAENCOF_META_DIR = '.maencof-meta';
/** 활동 기록 umbrella 디렉터리 — events / sessions / digests 를 담는다. */
export const ACTIVITY_DIR = 'activity';
/** 활동 이벤트 로그(NDJSON) 디렉터리. */
export const EVENTS_DIR = 'events';
/** 세션 레코드 디렉터리. */
export const SESSIONS_DIR = 'sessions';
/** 작업 이력 파생물(daily digest + 역색인) 디렉터리. */
export const DIGESTS_DIR = 'digests';
export const LAYER1_PREFIX = '01_Core';
/** 서고(그래프 밖 아카이브) 루트 디렉터리 이름. vault 런타임 경로이며 이 저장소 경로가 아니다. */
export const ARCHIVE_DIR = '99_Archive';
