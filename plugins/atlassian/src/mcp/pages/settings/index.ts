// Static settings UI assets (index.html + scripts/ + styles/). Served at
// runtime by `mcp/tools/setup` as HTML built by `scripts/buildSettingsHtml.mjs`.
// No runtime TypeScript exports are needed.
export {};
