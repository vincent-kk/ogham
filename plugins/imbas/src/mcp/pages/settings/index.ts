// Static settings UI assets (index.html + scripts/ + styles/). Served at
// runtime by `mcp/tools/openSettings` as an inlined HTML string built by
// `scripts/build-settings-html.mjs`. No runtime TypeScript exports are needed.
export {};
