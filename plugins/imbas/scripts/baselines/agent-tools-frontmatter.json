{
  "_note": "Baseline snapshot of agent `tools:` frontmatter arrays. Used by scripts/check-agent-tools-frontmatter.mjs to detect silent tool-grant expansion. Update this file only when intentionally changing agent permissions. MCP tool IDs use the full-form `mcp__plugin_<plugin>_<server>__<tool>` (server key `tools`) — short forms are not resolved for subagents.",
  "_captured_at": "2026-08-05",
  "_captured_in": "imbas v2 planner-side refactor (engineer removed)",
  "agents": {
    "planner": ["Read", "Grep", "Glob"],
    "analyst": ["Read", "Grep", "Glob", "Bash"]
  }
}
