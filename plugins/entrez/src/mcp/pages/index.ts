// Static FE build-input directory (settings/). No runtime TS exports;
// `scripts/buildSettingsHtml.mjs` inlines these sources into public/settings.html,
// which `mcp/tools/setup` serves at runtime.
export {};
