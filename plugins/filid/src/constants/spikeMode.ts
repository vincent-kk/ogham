/** Branch-name prefix that is the single authority for spike mode. */
export const SPIKE_BRANCH_PREFIX = 'spike/';

/** Recommended spike lifetime; exceeding it triggers banner emphasis. */
export const SPIKE_TIMEBOX_DAYS = 7;

/** Project-relative directory holding per-branch harvest manifests. */
export const HARVEST_DIR = '.filid/harvest';

/** Project-relative path of the acceptance-criteria ledger. */
export const CRITERIA_LEDGER_PATH = '.filid/criteria.md';
