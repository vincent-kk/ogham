/**
 * Canonical implementation lives in core/prSummary — this file existed as a
 * logic-identical duplicate. Re-export instead of re-implementing.
 */
export { extractRevalidateVerdict } from '../../../../core/prSummary/prSummary.js';
