# 수정요청서 — cross-review 판정이 정당한 제외를 커버리지 부족으로 취급함

- 대상: `plugins/filid/skills/cross-review` 및 이 스킬이 호출하는 `review_state` seal 경로
- 제기 근거: PR #123 (`filid/review-structure`) 리뷰 실행 결과
- 상태: 요구사항 정의 단계. 코드 스펙은 구현 시점에 확정한다.

## 1. 무슨 일이 있었나

46개 그룹 전체가 리뷰와 독립 검증을 마쳤다. 검증자가 지적 96건을 확정했고 3건을 반증했다. 판단 불가로 남은 지적은 없었다. 그런데 봉인이 낸 최종 판정은 `INCONCLUSIVE`였다.

| 항목           | 값  |
| -------------- | --- |
| 전체 파일      | 642 |
| 리뷰된 파일    | 542 |
| 제외된 파일    | 100 |
| 확정된 지적    | 96  |
| 반증된 지적    | 3   |
| 판단 불가 지적 | 0   |

제외된 100개는 모두 사유가 기록되어 있었다.

| 제외 사유            | 수  | 성격                                                                    |
| -------------------- | --- | ----------------------------------------------------------------------- |
| `deleted path`       | 97  | 이 브랜치에서 삭제된 경로. 현재 트리에 파일이 없다.                     |
| `generated artifact` | 3   | `bridge/`, `.codex-plugin/` 산출물. 저장소 규칙상 손편집 대상이 아니다. |

즉 커버리지 구멍이 아니다. 읽을 대상이 아예 없거나, 읽어서는 안 되는 파일이다.

## 2. 무엇이 문제인가

판정이 사실과 다른 기록을 남긴다.

- `INCONCLUSIVE`는 "리뷰가 결론을 내지 못했다"는 뜻이다. 이 실행은 결론을 냈다. 지적 96건이 독립 검증까지 통과했다.
- 실제로 필요한 후속 조치는 수정 요청이다. 그런데 판정만 보고 후속 단계를 정하는 사람이나 자동화는 이 PR을 "다시 리뷰해야 하는 상태"로 읽는다.
- 삭제된 파일이 많은 변경일수록 이 오분류가 심해진다. 구조 개편처럼 삭제 비중이 큰 PR은 아무리 리뷰를 충실히 해도 판정이 무조건 `INCONCLUSIVE`로 떨어진다. 리뷰 품질과 판정이 역상관하는 셈이다.
- 결과적으로 판정 값이 신호로서 쓸모를 잃는다. `INCONCLUSIVE`가 흔해지면 읽는 쪽이 무시하게 된다.

## 3. 요구사항

### R1. 사유가 선언된 제외는 커버리지 분모에서 뺀다

커버리지는 "리뷰 대상이었던 파일 중 실제로 리뷰된 비율"이어야 한다. 삭제된 경로와 생성 산출물은 애초에 대상이 아니므로 분모에 들어가면 안 된다. 사유 없이 빠진 파일만 커버리지 결손으로 센다.

### R2. 커버리지가 충족되면 판정은 지적이 결정한다

대상 파일을 모두 리뷰했다면, 확정된 지적이 하나라도 있을 때 `REQUEST_CHANGES`가 나와야 한다. 지적이 없으면 `APPROVED`다.

### R3. `INCONCLUSIVE`는 진짜 불확실에만 쓴다

다음 경우로 한정한다.

- 소진 처리되어 의견을 받지 못한 그룹이 있다
- 검증자가 판단 불가로 남긴 지적이 있다
- 사유 없이 리뷰되지 않은 파일이 있다
- 증거 수집 자체가 불완전하다

정당하게 제외된 파일은 여기에 해당하지 않는다.

### R4. 리포트가 제외 내역을 사유별로 보여준다

읽는 사람이 "왜 642개 중 542개만 봤는가"를 리포트 안에서 바로 확인할 수 있어야 한다. 사유별 건수와 대표 경로를 남긴다. 지금은 숫자만 있고 사유가 없어서, 상태 파일을 직접 열어봐야 알 수 있다.

### R5. 스킬 문서의 판정 정의를 함께 고친다

`report-formats.md`와 `SKILL.md`가 기술하는 판정 의미가 위 규칙과 어긋나지 않아야 한다. 코드만 고치고 문서를 두면 다음 사람이 다시 헷갈린다.

### R6. 회귀 테스트를 남긴다

최소한 다음 세 가지를 고정한다.

- 전부 삭제된 파일 + 확정 지적 있음 → `REQUEST_CHANGES`
- 사유 없는 미리뷰 파일 있음 → `INCONCLUSIVE`
- 대상 전부 리뷰 + 지적 없음 → `APPROVED`

## 4. 수용 기준

- 이번 실행과 동일한 입력을 다시 봉인하면 `REQUEST_CHANGES`가 나오고, 확정 지적 수는 96으로 유지된다.
- 리포트에 제외 사유별 집계가 표시된다.
- 기존 소진·재시도 경로의 동작은 바뀌지 않는다.

## 5. 범위 밖

- 그룹 분할 기준, 리스크 판정, 재시도 횟수는 건드리지 않는다.
- 이미 봉인된 산출물을 사후에 다시 쓰지 않는다. 재판정이 필요하면 새로 봉인한다.

## 6. 참고로 함께 관찰된 사항

이번 실행에서 리뷰어 의견이 스키마 검증에 걸린 사례가 네 번 있었다. `gaps` 항목을 문자열로 넣은 경우가 두 번, 지적 ID 형식이 어긋난 경우가 두 번이다. 모두 재실행으로 회복됐다. 판정 문제와는 별개지만, 브리프가 요구하는 출력 형식이 리뷰어에게 충분히 전달되지 않는다는 신호로 보인다. 별도 항목으로 다룰 가치가 있다.
