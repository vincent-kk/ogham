# 개발요청서 — maencof 그래프 외 서고와 경쟁 단위 교체 (2026-08-19)

ogham 리포(`plugins/maencof`) 대상 개발 의뢰. 2026-08-19 판정 세션의 결론을 구현 요구사항으로 정리한 것.

- 판정 전문: `[[maencof-qga-sa-랭킹계층-반영상태]]`
- 원칙: `[[llm-소비자-검색-도구의-목적함수는-예산-내-클러스터-커버리지]]`

## 배경 — 세 가지 문제

### P1. 증분 기록물이 수로 정제 지식을 밀어낸다

일자별 메일·Jira·일정 문서는 문서 수가 **사건 수**에 비례하고, 정제 지식(L2)은 **결론 수**에 비례한다. 1문서 1표로 경쟁시키면 사건 수가 결론 수를 이긴다. 같은 스레드 문서들은 티켓 번호 같은 희귀 토큰을 공유하므로 IDF로는 못 막는다.

검색 소비자가 후보 전체를 읽고 판단하는 LLM이므로, 목적함수는 상위 정밀도가 아니라 **토큰 예산 내 클러스터 커버리지**다 — 중복 1건이 들어올 때마다 다른 갈래의 대표 1건이 밀려난다.

### P2. "레이어 외 디렉토리는 그래프에서 무시"가 계약인데, 구현은 우연으로만 지킨다

스캔은 vault 전체 `**/*.md`를 돌고(제외: `.maencof/` · `.maencof-meta/` · `.obsidian/`), 노드 빌드는 경로를 검증하지 않고 frontmatter의 `layer`를 정본으로 쓴다. `99_Archive`의 72개 `.md`가 그래프에 없는 유일한 이유는 frontmatter 검증 실패다.

따라서 **유효한 frontmatter를 가진 문서를 `99_Archive`에 두면 그래프에 진입한다** — 서고(그래프 외 보관) 설계를 무력화하는 누수. 부작용으로 매 빌드 72건이 파싱 실패 노이즈로 기록된다.

### P3. 증류 후에도 원본이 침강하지 않는다

`archived` 스텁은 본문이 비어도 태그가 온전해 태그 채널 경쟁력을 유지한다(실측: `kg_suggest_links`에서 `archived` 스텁이 0.4점 1위 후보).

## 설계 원칙 — 경쟁 스펙트럼 3계급

판정 기준은 **"개별 항목이 질의 대상이 되는가"**.

| 계급 | 위치 | 경쟁권 | 예 |
| --- | --- | --- | --- |
| 간행물/원자료 | `99_Archive` (서고) | 0표 — 그래프 밖 | 뉴스레터 수집 원문, 회의 원자료 |
| 업무 에피소드 | L3/L4 | 스레드당 1표 | 메일·Jira·일정 ("GCC-3903 뭐였지"의 정답이 되므로 검색엔 남되, 뭉쳐서 1표) |
| 정제 지식 | L1/L2 | 문서당 1표 | 결정·원칙 문서 (현행 그대로) |

스레드 집계는 **max 승계, sum 금지** — 같은 스레드 문서 N건은 독립 증거가 아니라 같은 사건의 반복 관측이다. 합산하면 수 프리미엄이 부활한다.

## 요구사항

### 묶음 A — 서고 계약 승격 (독립 선행 가능)

- **R1. 스캔 allowlist**: 인덱싱 대상을 `01_Core` ~ `05_Context` 디렉토리로 한정한다. 제외 목록 나열(blocklist)이 아니라 **레이어 디렉토리 allowlist**로 — 미래의 낯선 디렉토리도 새지 않도록.
- **R2. 노드 빌드 경로 게이트**: 경로의 첫 세그먼트가 레이어 디렉토리가 아니면 노드 생성을 거부한다. 증분 빌드·lens 재수화 등 스캔 외 진입로까지 막는 2차 방어선.

### 묶음 B — 클러스터 collapse (R3 → R4 순서)

- **R3. `cluster_key` frontmatter 필드 신설**: create/update 도구 스키마에 노출. 증분 문서가 자기 스레드를 선언하는 용도(예: `cluster_key: jira-gcc-3903`, `cluster_key: works-mail-2026-08-19`). 시드·태그 채널과 완전히 분리된 별도 필드.
- **R4. 검색 시 클러스터 collapse**: `kg_search` / `kg_context` 결과에서 같은 `cluster_key`를 가진 문서들을 **대표 1건**으로 접는다.
  - 그룹 점수는 **max 승계**.
  - 대표는 클러스터 내 `updated` 최신 문서(증류본이 생기면 자동으로 대표 승계).
  - 응답에 **접힌 건수**를 표기해 호출자(LLM)가 필요 시 해당 클러스터를 명시 질의로 열 수 있게 한다.
  - `cluster_key` 없는 문서는 현행대로 개별 경쟁.

### 묶음 C — 소형 정리

- **R5. `archived` 시드 강등**: `archived: true` 문서는 시드 매칭 점수를 강등한다 (증류 후 침강 완성).
- **R6. checkup 링크 예외**: 레이어 문서에서 `99_Archive` 경로로의 링크는 깨진 링크가 아니라 **서고 참조**로 분류한다.

### 묶음 D — 검증

- **R7. 커버리지 골든 케이스**: "같은 `cluster_key` 문서 N건 + L2 결정 1건 + 인접 주제 1건" 구성에서 **대표 1건 + 결정 + 인접**이 top-k에 오는지 측정하는 골든 쿼리를 추가한다. 기존 골든셋 ratchet 규율 유지 — 회귀 없이 통과해야 한다.

## 수용 기준

1. 유효 frontmatter 문서를 `99_Archive`에 두어도 그래프에 진입하지 않는다 (R1·R2).
2. 빌드 시 `99_Archive`발 파싱 실패 노이즈가 0건이다 (R1).
3. 같은 `cluster_key` 문서 8건이 있어도 top-10에는 그 클러스터의 대표 1건만 나타나고, 접힌 건수가 응답에 표기된다 (R3·R4).
4. `archived` 문서가 시드 후보 상위에 오르지 않는다 (R5).
5. 기존 골든셋 ratchet 통과 — nDCG/Recall 회귀 없음 (R7).
